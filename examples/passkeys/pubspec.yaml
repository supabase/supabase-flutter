name: passkeys_example
description: Demonstrates passkey (WebAuthn) authentication with supabase_flutter.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.35.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  passkeys: ^2.21.1
  supabase_flutter: ^2.15.4

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
