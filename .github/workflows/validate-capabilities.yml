name: SDK Compliance

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    permissions:
      contents: read
      pull-requests: write
    uses: supabase/sdk/.github/workflows/validate-sdk-compliance-dart.yml@5bd367cd2a88a1d99ed3060154069caf0381d5ff # capability-matrix/v1.13.0