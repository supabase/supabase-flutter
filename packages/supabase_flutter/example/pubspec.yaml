name: supabase_flutter_example
description: Demonstrates how to use supabase_flutter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  supabase_lints: ^0.1.1

flutter:
  uses-material-design: true
