# supabase_testing

Test helpers for apps and packages built on the Supabase Dart and Flutter
clients. Holds the mock HTTP clients, JWT builders, auth fixtures and realtime
frames that the test suites of the client packages themselves run on, so your
tests can build on the same primitives instead of hand-rolling them.
