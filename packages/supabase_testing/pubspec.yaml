name: supabase_testing
description: Test helpers for apps built on the Supabase Dart and Flutter clients, with mock HTTP clients, JWT builders and auth fixtures.
version: 0.1.0
homepage: 'https://supabase.com'
repository: 'https://github.com/supabase/supabase-flutter/tree/main/packages/supabase_testing'
issue_tracker: 'https://github.com/supabase/supabase-flutter/issues'
topics:
  - supabase
  - testing
  - mock

environment:
  sdk: '>=3.9.0 <4.0.0'

resolution: workspace

dependencies:
  crypto: ^3.0.7
  http: ^1.6.0
  meta: ^1.16.0

dev_dependencies:
  supabase_lints: ^0.1.1
  test: ^1.25.0
